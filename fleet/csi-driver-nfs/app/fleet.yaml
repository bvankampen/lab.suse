defaultNamespace: csi-driver-nfs

helm:
  chart: oci://registry.lab.suse/dp.apps.rancher.io/charts/kubernetes-csi-driver-nfs
  releaseName: kubernetes-csi-driver-nfs
  version: 4.13.4

overrideTargets:
  - clusterSelector:
      matchLabels:
        fleet-app-csi-driver-nfs: "true"
