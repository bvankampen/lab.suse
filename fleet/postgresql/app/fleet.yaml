defaultNamespace: postgresql

helm:
  chart: oci://registry.lab.suse/dp.apps.rancher.io/charts/postgresql
  releaseName: postgresql
  version: 0.6.1
  valuesFiles:
    - values.yaml

overrideTargets:
  - clusterSelector:
      matchLabels:
        fleet-app-postgresql: "true"
