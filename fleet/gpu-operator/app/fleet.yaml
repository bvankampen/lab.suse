defaultNamespace: gpu-operator-system

helm:
  repo: https://helm.ngc.nvidia.com/nvidia
  chart: gpu-operator
  # chart: oci://registry.lab.suse/charts/gpu-operator
  # version: 25.10.1
  version: v26.3.0
  releaseName: gpu-operator
  valuesFiles:
    - values.yaml

overrideTargets:
  - clusterSelector:
      matchLabels:
        fleet-app-gpu-operator: "true"
