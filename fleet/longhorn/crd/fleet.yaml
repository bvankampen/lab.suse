defaultNamespace: longhorn-system

helm:
  repo: https://charts.rancher.io
  chart: longhorn-crd
  releaseName: longhorn-crd
  version: 108.2.0+up1.10.1

overrideTargets:
  - clusterSelector:
      matchLabels:
        fleet-app-longhorn: "true"
