defaultNamespace: external-dns-system

helm:
  chart: oci://registry.lab.suse/dp.apps.rancher.io/charts/external-dns
  releaseName: external-dns
  version: 1.19.0
  valuesFiles:
    - values.yaml

overrideTargets:
  - clusterSelector:
      matchLabels:
        fleet-app-external-dns: "true"
