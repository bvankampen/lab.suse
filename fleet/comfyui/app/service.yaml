apiVersion: v1
kind: Service
metadata:
  name: comfyui-service
  labels:
    app.kubernetes.io/name: comfyui
spec:
  type: ClusterIP
  ports:
    - port: 8080
      name: http
  selector:
    app.kubernetes.io/name: comfyui
