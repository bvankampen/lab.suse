apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: suse-registry
spec:
  refreshInterval: "300s"
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: suse-registry
  data:
    - secretKey: user
      remoteRef:
        key: secretstore.lab.suse
        property: suse-registry-user
    - secretKey: token
      remoteRef:
        key: secretstore.lab.suse
        property: suse-registry-token
