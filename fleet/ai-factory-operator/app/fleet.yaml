defaultNamespace: aif-operator

helm:
  chart: oci://ghcr.io/suse/chart/aif-operator
  releaseName: aif-operator
  version: 2.1.0
  takeOwnership: true
  valuesFiles:
    - values.yaml

overrideTargets:
  - clusterSelector:
      matchLabels:
        fleet-app-aif-operator: "true"
