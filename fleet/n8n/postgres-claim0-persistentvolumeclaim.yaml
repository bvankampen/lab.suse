kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: postgresql-pv
  namespace: n8n
spec:
  storageClass: local-path-nvme
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
