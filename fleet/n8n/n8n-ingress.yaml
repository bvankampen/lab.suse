apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: n8n
  annotations:
    cert-manager.io/cluster-issuer: local-issuer
spec:
  ingressClassName: traefik
  rules:
    - host: n8n.lab.suse
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: n8n
                port:
                  number: 5678
  tls:
    - hosts:
        - n8n.lab.suse
      secretName: tls-n8n
