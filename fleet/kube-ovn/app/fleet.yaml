defaultNamespace: kube-system

helm:
  chart: oci://registry.lab.suse/ghcr.io/kubeovn/charts/kube-ovn-v2
  releaseName: kube-ovn
  version: 1.16.2
  valuesFiles:
    - values.yaml

overrideTargets:
  - clusterSelector:
      matchLabels:
        fleet-app-kube-ovn: "true"
