defaultNamespace: external-secrets-system

helm:
  chart: oci://registry.lab.suse/dp.apps.rancher.io/charts/external-secrets-operator
  releaseName: external-secrets
  version: 2.9.0

overrideTargets:
  - clusterSelector:
      matchLabels:
        fleet-app-external-secrets: "true"
